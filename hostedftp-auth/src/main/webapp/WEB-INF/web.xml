<?xml version="1.0" encoding="UTF-8"?>
<web-app
    xmlns="https://jakarta.ee/xml/ns/jakartaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
    version="6.0">

  <display-name>hostedftp-auth</display-name>

  <!-- Your simple welcome file (index.jsp can forward to /home) -->
  <welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>

  <!-- 30-minute sessions -->
  <session-config>
    <session-timeout>30</session-timeout>
  </session-config>

  <!-- No <servlet> / <servlet-mapping> / <filter> here.
       We use annotations:
         @WebServlet("/login"), @WebServlet("/register"), @WebServlet("/home")
         @WebFilter(urlPatterns = {"/home", "/account/*"})
  -->
</web-app>
