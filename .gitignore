# -----------------------------
# OS / Editor cruft
# -----------------------------
.DS_Store
.AppleDouble
Thumbs.db
.vscode/
.idea/
*.iml
.classpath
.project
.settings/

# -----------------------------
# Maven / Java build artifacts
# -----------------------------
target/
*.class
*.war
*.ear
*.jar
*.log

# If you have multiple modules (like hostedftp-auth), this still ignores their targets
**/target/

# -----------------------------
# Node/Frontend (if any sneaks in)
# -----------------------------
node_modules/
dist/
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml

# -----------------------------
# Tomcat (in case anything gets copied into the repo by mistake)
# -----------------------------
webapps/
work/
logs/
temp/

# -----------------------------
# Local config / secrets (NEVER COMMIT)
# -----------------------------
*.env
*.env.*
.envrc
*.local.properties
*.secrets
*.keystore
*.jks
*.p12
*.pem
*.crt
*.key

# If you ever keep a local setenv in the project (Tomcat's real one lives under CATALINA_HOME/bin)
setenv.sh

# -----------------------------
# SQL dumps (keep your curated sql-scripts/ tracked)
# -----------------------------
*.sql
!sql-scripts/**

# -----------------------------
# Misc caches / coverage
# -----------------------------
**/.cache/
coverage/
jacoco.exec

# -----------------------------
# IntelliJ / Eclipse workspace noise (some editors recreate on open)
# -----------------------------
.idea/**/workspace.xml
.idea/**/tasks.xml
.idea/**/dictionaries
.idea/**/shelf
.idea/**/httpRequests
